---
// src/pages/partners/[slug].astro — Partner Architecture Framework & Decision Layer (Parts XVII & XVIII)
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const partners = await getCollection('partners');
  return partners
    .filter(partner => partner.data.partnerPageEnabled && partner.data.relationshipConfirmed && partner.data.publicNameApproved)
    .map(partner => ({
      params: { slug: partner.slug },
      props: { partner }
    }));
}

const { partner } = Astro.props;
const { Content } = await partner.render();

// Bespoke Signature Technical Blocks & Buyer Decision Guidance (Parts XVII & XVIII)
const partnerSignatures: Record<string, {
  architectureTitle: string;
  architectureSubtitle: string;
  steps: { num: string; title: string; desc: string }[];
  decisionQuestions: string[];
  whenFits: string;
  whenAlternative: string;
  commercialCaution: string;
}> = {
  fastnetmon: {
    architectureTitle: "Detection & Mitigation Control Flow",
    architectureSubtitle: "Where FastNetMon can sit between network telemetry, routing policy, and external mitigation",
    steps: [
      { num: "01", title: "Traffic Telemetry", desc: "Processes supported network telemetry and traffic sources so operators can observe potential DDoS events." },
      { num: "02", title: "Detection", desc: "Analyzes traffic patterns to identify attacks and the affected destinations." },
      { num: "03", title: "Routing Response", desc: "Can automate policy-driven actions such as BGP FlowSpec or remote-triggered blackholing (RTBH)." },
      { num: "04", title: "External Mitigation", desc: "Can support diversion workflows toward external scrubbing services when the network design requires them." }
    ],
    decisionQuestions: [
      "Which telemetry sources are available at the network edge?",
      "Which FlowSpec or RTBH actions are supported by your routers and upstream providers?",
      "Who should own mitigation policy and escalation during an attack?"
    ],
    whenFits: "Network operators that want a detection and control layer while retaining responsibility for routing and mitigation policy.",
    whenAlternative: "A fully managed protection service may fit better when the buyer does not want to operate the detection and routing-control layer.",
    commercialCaution: "Confirm router support, upstream routing policy, licensing scope, and any external scrubbing handoff before relying on an automated response design."
  },
  gcore: {
    architectureTitle: "Cloud, Edge & Security Service Map",
    architectureSubtitle: "How Gcore's compute, AI, delivery, networking, and security services can relate to one requirement",
    steps: [
      { num: "01", title: "Compute & Bare Metal", desc: "Evaluate virtual machines, bare metal, storage, containers, databases, and cloud networking against the workload." },
      { num: "02", title: "AI Infrastructure", desc: "Consider GPU and AI services where accelerator access, inference, or private AI infrastructure is part of the requirement." },
      { num: "03", title: "Edge Delivery", desc: "CDN, DNS, streaming, and edge-delivery services may matter when content or application distribution is part of the design." },
      { num: "04", title: "Security", desc: "DDoS protection, WAF, and bot-management services can be evaluated alongside the infrastructure and delivery path." }
    ],
    decisionQuestions: [
      "Which regions and workload types actually need Gcore services?",
      "Is the requirement compute-led, delivery-led, security-led, or a combination?",
      "What network, egress, data-location, and operational constraints should be compared?"
    ],
    whenFits: "Requirements that combine cloud or bare-metal infrastructure with distributed delivery, AI services, or network and application security.",
    whenAlternative: "A narrower regional provider or a hyperscaler-specific managed service may fit better when the workload is tightly coupled to one platform or geography.",
    commercialCaution: "Compare region availability, service-specific pricing, traffic charges, support scope, and contract terms for the exact configuration."
  },
  stormwall: {
    architectureTitle: "Managed DDoS & Application Protection",
    architectureSubtitle: "Where network diversion, TCP/UDP protection, WAF, and antibot services can sit in the protection path",
    steps: [
      { num: "01", title: "Protection Scope", desc: "Define whether the requirement is network-layer, TCP/UDP service, web-application, or combined protection." },
      { num: "02", title: "Traffic Onboarding", desc: "Establish how protected traffic reaches the service, including routing or application-layer integration where applicable." },
      { num: "03", title: "Filtering", desc: "The protection service filters malicious traffic according to the selected network or application protection model." },
      { num: "04", title: "Service Delivery", desc: "Confirm how legitimate traffic returns to the protected service and how monitoring, escalation, and support are handled." }
    ],
    decisionQuestions: [
      "Is the requirement always-on protection, on-demand mitigation, or application-layer protection?",
      "Which protocols, applications, and traffic volumes must be protected?",
      "What return path, operational handoff, and escalation model does the design require?"
    ],
    whenFits: "Organizations that need an external managed DDoS or web-application protection service rather than operating the entire mitigation stack themselves.",
    whenAlternative: "A detection-and-control platform may fit better when the network operator wants to retain its own routing policy and use third-party mitigation only when required.",
    commercialCaution: "Confirm onboarding method, protected capacity, support scope, service commitments, and any clean-traffic return requirements before contracting."
  },
  zenlayer: {
    architectureTitle: "Distributed Compute & Connectivity",
    architectureSubtitle: "How bare metal, cloud networking, IP transit, acceleration, and edge services can be combined",
    steps: [
      { num: "01", title: "Compute Placement", desc: "Evaluate bare-metal or elastic compute against workload location, hardware, and operational requirements." },
      { num: "02", title: "Private Connectivity", desc: "Cloud networking and private-connectivity services can link workloads, cloud environments, and distributed locations." },
      { num: "03", title: "Internet & Acceleration", desc: "IP transit, acceleration, and CDN services may be relevant where internet reach and application delivery are part of the requirement." },
      { num: "04", title: "Edge Infrastructure", desc: "Edge colocation and distributed infrastructure options can be evaluated where physical presence or regional placement matters." }
    ],
    decisionQuestions: [
      "Which metros and user regions actually drive the requirement?",
      "Does the workload need bare metal, elastic compute, private connectivity, or several of these together?",
      "Which latency, traffic, routing, and contract constraints should be tested against available locations?"
    ],
    whenFits: "Multi-region requirements where compute placement and connectivity need to be considered together.",
    whenAlternative: "A local provider may be simpler when the workload is concentrated in one market and does not need a distributed infrastructure footprint.",
    commercialCaution: "Confirm location-specific availability, port and traffic terms, minimum commitments, and deployment lead times for the selected services."
  },
  ipxo: {
    architectureTitle: "IPv4 Sourcing & Governance Flow",
    architectureSubtitle: "From address requirements through authorization, routing preparation, reputation, and abuse management",
    steps: [
      { num: "01", title: "Requirements & Search", desc: "Define prefix size, region, lease duration, and intended use before evaluating available IPv4 resources." },
      { num: "02", title: "Authorization & Governance", desc: "Review the applicable authorization, KYC, RPKI, and governance steps for the selected resource." },
      { num: "03", title: "Routing Preparation", desc: "Confirm LOA, ROA, upstream acceptance, and routing requirements before announcing a leased prefix." },
      { num: "04", title: "Reputation & Abuse", desc: "Monitor reputation and abuse history throughout the life of the resource rather than treating availability alone as sufficient." }
    ],
    decisionQuestions: [
      "What prefix size and region are actually required?",
      "Who controls the LOA, ROA, and routing workflow for the selected resource?",
      "What reputation, abuse-history, and upstream-acceptance checks are required before use?"
    ],
    whenFits: "Organizations that need additional IPv4 resources or want to manage and monetize unused address space without treating address availability as the only decision factor.",
    whenAlternative: "IPv6-first or dual-stack architecture may reduce the amount of IPv4 space required where the application and user base support it.",
    commercialCaution: "Check lease terms, authorization, reputation, abuse handling, geolocation expectations, and routing acceptance for the actual prefix."
  },
  vates: {
    architectureTitle: "XCP-ng & Xen Orchestra Stack",
    architectureSubtitle: "How the hypervisor, management, backup, replication, and support layers fit together",
    steps: [
      { num: "01", title: "XCP-ng", desc: "Provides the open-source virtualization platform based on the Xen Project hypervisor." },
      { num: "02", title: "Xen Orchestra", desc: "Adds browser and API-based management across XCP-ng environments." },
      { num: "03", title: "Backup & Replication", desc: "Evaluate the backup, replication, metrics, and automation capabilities required by the operating model." },
      { num: "04", title: "Support & Operations", desc: "Define the commercial support, migration planning, hardware, storage, and operational requirements around the virtualization stack." }
    ],
    decisionQuestions: [
      "Which VMware or virtualization features are actually in use today?",
      "What storage, networking, HA, backup, and recovery requirements must the target platform satisfy?",
      "What migration testing and operational training will be required?"
    ],
    whenFits: "Enterprises, hosting providers, and cloud operators evaluating an open-source virtualization stack with commercial support options.",
    whenAlternative: "A KVM-based platform or another virtualization approach may fit better when existing skills, tooling, or workload dependencies point elsewhere.",
    commercialCaution: "Map feature dependencies, storage and network design, support scope, and migration testing before committing to a platform change."
  },
  itcare: {
    architectureTitle: "Managed Network Operations Workflow",
    architectureSubtitle: "How monitoring, triage, engineering, automation, and carrier coordination can fit together",
    steps: [
      { num: "01", title: "Monitoring", desc: "Define the devices, links, services, and alert sources covered by the managed operating model." },
      { num: "02", title: "Triage", desc: "Establish how incidents are classified, correlated, and escalated across the agreed service scope." },
      { num: "03", title: "Engineering & Automation", desc: "Use network engineering, DevOps, and automation where the operating model calls for configuration or workflow changes." },
      { num: "04", title: "Escalation & Coordination", desc: "Define handoff boundaries between the managed team, internal engineers, carriers, and other infrastructure providers." }
    ],
    decisionQuestions: [
      "Which hours, devices, services, and incident classes need external operational coverage?",
      "Which actions may be automated and which require engineer approval?",
      "What escalation path and authority boundaries should apply during a network incident?"
    ],
    whenFits: "Infrastructure operators that want to augment internal network engineering with managed NOC, automation, or operational support.",
    whenAlternative: "A fully internal operations model may be preferable when the organization already has sufficient round-the-clock coverage and specialist skills.",
    commercialCaution: "Define runbooks, access controls, escalation targets, maintenance responsibilities, and change authority before handoff."
  },
  airframe: {
    architectureTitle: "Technology Evaluation & Diligence Flow",
    architectureSubtitle: "From requirements and market mapping through evidence, comparison, and build-versus-buy decisions",
    steps: [
      { num: "01", title: "Requirement Framing", desc: "Clarify the objectives, constraints, and decision criteria before comparing vendors or build options." },
      { num: "02", title: "Market Mapping", desc: "Research relevant companies, products, pricing structures, and capability differences for the defined requirement." },
      { num: "03", title: "Evidence & Diligence", desc: "Compare vendor claims with available implementation evidence, pricing intelligence, and practitioner feedback." },
      { num: "04", title: "Decision Support", desc: "Distinguish what is supported by evidence, what remains uncertain, and what should be validated before a build-versus-buy decision." }
    ],
    decisionQuestions: [
      "Is the team evaluating a material software or AI platform commitment?",
      "Which evidence is needed beyond vendor marketing to defend the decision?",
      "Is the real choice between buying a product, building internally, or narrowing the requirement first?"
    ],
    whenFits: "Technology teams that need structured market intelligence or vendor diligence before a consequential software or AI decision.",
    whenAlternative: "For a routine commodity purchase with clear requirements and low switching cost, formal diligence may be disproportionate.",
    commercialCaution: "Start diligence early enough that evidence can shape the shortlist before commercial negotiations narrow the options."
  }
};

const signature = partnerSignatures[partner.slug] || {
  architectureTitle: "Technical Fit & Operating Model",
  architectureSubtitle: `How to evaluate ${partner.data.name} against the requirement`,
  steps: [
    { num: "01", title: "Requirement", desc: "Clarify the technical and operational requirement before comparing providers." },
    { num: "02", title: "Capability Fit", desc: "Compare the provider's published capabilities with the actual workload and constraints." },
    { num: "03", title: "Commercial Terms", desc: "Confirm provider-specific pricing, support, service commitments, and contract terms." },
    { num: "04", title: "Delivery", desc: "Define implementation, handoff, and ongoing operational responsibilities before contracting." }
  ],
  decisionQuestions: [
    "What technical constraints must the provider satisfy?",
    "Which support, service, and commercial terms materially affect the decision?",
    "What responsibilities remain with your team after implementation?"
  ],
  whenFits: "Requirements that align with the provider's published capability scope and operating model.",
  whenAlternative: "Another approach may fit better when the workload, geography, operating model, or commercial constraints point elsewhere.",
  commercialCaution: "Confirm provider-specific commercial and service terms before contracting."
};
---

<Layout 
  title={`${partner.data.name} — Specialist Partner Profile | InfraHub`}
  description={partner.data.tagline}
  canonicalPath={`/partners/${partner.slug}`}
>
  <div class="partner-profile-page">
    <div class="container partner-profile-container">
      
      <!-- Profile Header / Identity Ribbon -->
      <header class="profile-header">
        <div class="header-breadcrumb-row">
          <a href="/partners" class="breadcrumb-link">&larr; Partner Ecosystem Ledger</a>
          <span class="breadcrumb-sep">/</span>
          <span class="breadcrumb-current">{partner.data.name}</span>
        </div>

        <div class="header-main-flex">
          <div class="header-brand-box">
            {partner.data.logoApproved && partner.data.logoStatus === 'approved' && partner.data.logo ? (
              <img 
                src={partner.data.logo} 
                alt={`${partner.data.name} logo`} 
                class="profile-logo-img"
                loading="eager"
                width="160"
                height="36"
              />
            ) : (
              <span class="profile-text-mark">{partner.data.name}</span>
            )}
            <div class="header-title-block">
              <h1 class="profile-title">{partner.data.name}</h1>
              <p class="profile-short-role">{partner.data.shortRole || partner.data.tagline}</p>
            </div>
          </div>

          <div class="header-meta-actions">
            <a 
              href={partner.data.officialWebsite} 
              class="btn btn-secondary btn-sm"
              target="_blank" 
              rel="noopener noreferrer"
            >
              <span>Corporate Website</span>
              <span class="ext-icon" aria-hidden="true">&nearr;</span>
            </a>
            <a href={`/lets-talk?partner=${partner.slug}`} class="btn btn-accent btn-sm">
              <span>Discuss Requirement</span>
            </a>
          </div>
        </div>

        <p class="profile-tagline measure-lead">{partner.data.tagline}</p>
      </header>

      <!-- Signature Technical Architecture Block (Part XVII: Subject-Specific Architecture) -->
      <section class="signature-architecture-section">
        <div class="sig-header">
          <div class="sig-kicker-row">
            <span class="routing-junction" aria-hidden="true"></span>
            <span class="sig-kicker">TECHNICAL ARCHITECTURE // SIGNATURE INTEGRATION</span>
          </div>
          <h2 class="sig-title">{signature.architectureTitle}</h2>
          <p class="sig-subtitle measure-technical">{signature.architectureSubtitle}</p>
        </div>

        <div class="sig-steps-grid">
          {signature.steps.map(step => (
            <div class="sig-step-card">
              <span class="sig-step-num">{step.num}</span>
              <h3 class="sig-step-title">{step.title}</h3>
              <p class="sig-step-desc">{step.desc}</p>
            </div>
          ))}
        </div>
      </section>

      <!-- The Buyer-Decision Layer (Part XVIII: Questions We Ask First, When Fits, Commercial Caution) -->
      <section class="buyer-decision-layer">
        <div class="decision-header">
          <span class="decision-kicker">INFRASTRUCTURE FIELD GUIDE // BUYER DECISION CRITERIA</span>
          <h2 class="decision-title">What Changes the Answer</h2>
          <p class="decision-subhead measure-lead">
            We evaluate specialist providers based on operational constraints rather than marketing catalogs. Consider these criteria before contracting.
          </p>
        </div>

        <div class="decision-triad-grid">
          <!-- Column 1: Questions We Ask First -->
          <div class="decision-col-card">
            <div class="col-indicator-bar" style="background: var(--accent-blue);"></div>
            <h3 class="decision-col-title">Questions We Ask First</h3>
            <ul class="decision-questions-list">
              {signature.decisionQuestions.map(q => (
                <li>
                  <span class="q-marker" aria-hidden="true">&rarr;</span>
                  <span>{q}</span>
                </li>
              ))}
            </ul>
          </div>

          <!-- Column 2: When This Fits vs Alternative -->
          <div class="decision-col-card">
            <div class="col-indicator-bar" style="background: var(--accent-cyan);"></div>
            <h3 class="decision-col-title">Workload Suitability</h3>
            <div class="fit-block">
              <span class="fit-tag tag-fits">WHEN THIS FITS:</span>
              <p class="fit-text">{signature.whenFits}</p>
            </div>
            <div class="fit-block" style="margin-top: 14px;">
              <span class="fit-tag tag-alt">WHEN AN ALTERNATIVE FITS:</span>
              <p class="fit-text">{signature.whenAlternative}</p>
            </div>
          </div>

          <!-- Column 3: Commercial & Operational Caution -->
          <div class="decision-col-card">
            <div class="col-indicator-bar" style="background: var(--accent-amber);"></div>
            <h3 class="decision-col-title">What Buyers Often Miss</h3>
            <div class="caution-box">
              <span class="caution-kicker">COMMERCIAL &amp; SOURCING FACTOR</span>
              <p class="caution-text">{signature.commercialCaution}</p>
            </div>
            <p class="advisory-reassurance">
              InfraHub reviews these constraints during technical qualification before provider introduction.
            </p>
          </div>
        </div>
      </section>

      <!-- Editorial Narrative Prose & Sourced Capabilities -->
      <section class="partner-prose-section">
        <div class="prose-layout-grid">
          
          <!-- Left: Narrative Prose Content -->
          <div class="prose-main-col">
            <div class="markdown-body">
              <Content />
            </div>

            <!-- Verified Capabilities Block -->
            <div class="verified-caps-block">
              <h3 class="caps-block-title">Published capability scope</h3>
              <p class="caps-block-note">
                Descriptions below are aligned to {partner.data.name}'s current public materials:
              </p>
              <ul class="caps-detail-list">
                {partner.data.capabilities.map(cap => (
                  <li>
                    <span class="cap-bullet" aria-hidden="true">&check;</span>
                    <span>{cap}</span>
                  </li>
                ))}
              </ul>
            </div>
          </div>

          <!-- Right: Direct Consultation & Governance Sidebar -->
          <aside class="prose-sidebar-col">
            <div class="consultation-card">
              <span class="consult-kicker">QUALIFIED INTRODUCTION</span>
              <h3 class="consult-title">Evaluate {partner.data.name}</h3>
              <p class="consult-desc">
                Discuss the requirement with InfraHub. We help clarify technical constraints, compare relevant options, and coordinate an introduction. Provider-specific pricing, availability, and service commitments are confirmed before contracting.
              </p>
              <a href={`/lets-talk?partner=${partner.slug}`} class="btn btn-accent btn-block btn-lg">
                <span>Discuss Your Requirement</span>
              </a>

              <div class="governance-assurances-list">
                <div class="assurance-item">
                  <span class="assure-icon">&check;</span>
                  <span>Requirement-led architectural evaluation</span>
                </div>
                <div class="assurance-item">
                  <span class="assure-icon">&check;</span>
                  <span>Provider-specific commercial terms</span>
                </div>
                <div class="assurance-item">
                  <span class="assure-icon">&check;</span>
                  <span>Direct introduction to the selected specialist</span>
                </div>
              </div>
            </div>
          </aside>

        </div>
      </section>

    </div>
  </div>
</Layout>

<style>
  .partner-profile-page {
    background-color: var(--bg-primary);
    padding: 48px 0 96px;
  }

  .partner-profile-container {
    max-width: var(--max-width);
  }

  /* Header */
  .profile-header {
    border-bottom: 1px solid var(--border-color);
    padding-bottom: 36px;
    margin-bottom: 56px;
  }

  .header-breadcrumb-row {
    display: flex;
    align-items: center;
    gap: 8px;
    font-family: var(--font-mono);
    font-size: 0.78rem;
    color: var(--text-muted);
    margin-bottom: 24px;
  }

  .breadcrumb-link {
    color: var(--accent-blue);
    text-decoration: none;
    font-weight: 600;
  }

  .breadcrumb-link:hover {
    text-decoration: underline;
  }

  .header-main-flex {
    display: flex;
    flex-direction: column;
    gap: 20px;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 20px;
  }

  @media (min-width: 900px) {
    .header-main-flex {
      flex-direction: row;
      align-items: center;
    }
  }

  .header-brand-box {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  @media (min-width: 600px) {
    .header-brand-box {
      flex-direction: row;
      align-items: center;
      gap: 24px;
    }
  }

  .profile-logo-img {
    height: 32px;
    width: auto;
    max-width: 160px;
    object-fit: contain;
    display: block;
  }

  .profile-text-mark {
    font-family: var(--font-heading);
    font-size: 1.8rem;
    font-weight: 700;
    color: var(--text-primary);
  }

  .profile-title {
    font-size: 2rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0;
    line-height: 1.2;
  }

  .profile-short-role {
    font-family: var(--font-mono);
    font-size: 0.82rem;
    color: var(--accent-blue);
    margin: 2px 0 0;
    font-weight: 500;
  }

  .header-meta-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
  }

  .ext-icon {
    font-size: 0.82rem;
  }

  .profile-tagline {
    font-size: 1.15rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0;
  }

  /* Signature Architecture Section */
  .signature-architecture-section {
    background-color: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-sm);
    padding: 40px;
    margin-bottom: 56px;
  }

  .sig-header {
    margin-bottom: 32px;
  }

  .sig-kicker-row {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 8px;
  }

  .sig-kicker {
    font-family: var(--font-mono);
    font-size: 0.74rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    color: var(--accent-blue);
    text-transform: uppercase;
  }

  .sig-title {
    font-size: 1.6rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0 0 6px;
  }

  .sig-subtitle {
    font-size: 0.95rem;
    color: var(--text-muted);
    margin: 0;
  }

  .sig-steps-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 20px;
  }

  @media (min-width: 860px) {
    .sig-steps-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .sig-step-card {
    background-color: var(--bg-primary);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-sm);
    padding: 20px;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .sig-step-num {
    font-family: var(--font-mono);
    font-size: 0.78rem;
    font-weight: 700;
    color: var(--accent-blue);
  }

  .sig-step-title {
    font-size: 1rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0;
    line-height: 1.35;
  }

  .sig-step-desc {
    font-size: 0.84rem;
    color: var(--text-secondary);
    line-height: 1.5;
    margin: 0;
  }

  /* Buyer Decision Layer */
  .buyer-decision-layer {
    margin-bottom: 64px;
    border-top: 2px solid var(--text-primary);
    padding-top: 36px;
  }

  .decision-header {
    margin-bottom: 32px;
  }

  .decision-kicker {
    font-family: var(--font-mono);
    font-size: 0.74rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    color: var(--accent-blue);
    text-transform: uppercase;
    display: block;
    margin-bottom: 8px;
  }

  .decision-title {
    font-size: 1.8rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0 0 8px;
  }

  .decision-subhead {
    font-size: 1.05rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0;
  }

  .decision-triad-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 24px;
  }

  @media (min-width: 900px) {
    .decision-triad-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .decision-col-card {
    background-color: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-sm);
    padding: 24px;
    display: flex;
    flex-direction: column;
    gap: 14px;
    position: relative;
    overflow: hidden;
  }

  .col-indicator-bar {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
  }

  .decision-col-title {
    font-size: 1.12rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0;
  }

  .decision-questions-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 10px;
    font-size: 0.88rem;
    color: var(--text-secondary);
    line-height: 1.45;
  }

  .decision-questions-list li {
    display: flex;
    align-items: baseline;
    gap: 8px;
  }

  .q-marker {
    color: var(--accent-blue);
    font-weight: 700;
    flex-shrink: 0;
  }

  .fit-block {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .fit-tag {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.04em;
  }

  .tag-fits { color: var(--accent-cyan); }
  .tag-alt { color: var(--text-muted); }

  .fit-text {
    font-size: 0.86rem;
    color: var(--text-secondary);
    line-height: 1.5;
    margin: 0;
  }

  .caution-box {
    background-color: var(--bg-primary);
    border: 1px solid var(--border-color);
    border-left: 2px solid var(--accent-amber);
    padding: 12px 14px;
    border-radius: var(--radius-sm);
  }

  .caution-kicker {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    font-weight: 700;
    color: var(--accent-amber);
    letter-spacing: 0.04em;
    display: block;
    margin-bottom: 4px;
  }

  .caution-text {
    font-size: 0.84rem;
    color: var(--text-secondary);
    line-height: 1.45;
    margin: 0;
  }

  .advisory-reassurance {
    font-size: 0.8rem;
    color: var(--text-muted);
    line-height: 1.4;
    margin: 0;
  }

  /* Prose & Sidebar Layout */
  .prose-layout-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 40px;
  }

  @media (min-width: 900px) {
    .prose-layout-grid {
      grid-template-columns: 1fr 340px;
      align-items: start;
    }
  }

  .markdown-body {
    font-size: 1rem;
    line-height: 1.7;
    color: var(--text-secondary);
  }

  .markdown-body h2 {
    font-size: 1.45rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 32px 0 14px;
    border-bottom: 1px solid var(--border-color);
    padding-bottom: 8px;
  }

  .markdown-body h3 {
    font-size: 1.18rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 24px 0 10px;
  }

  .markdown-body p {
    margin-bottom: 16px;
  }

  .verified-caps-block {
    margin-top: 40px;
    border-top: 1px solid var(--border-color);
    padding-top: 24px;
  }

  .caps-block-title {
    font-size: 1.2rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0 0 6px;
  }

  .caps-block-note {
    font-size: 0.88rem;
    color: var(--text-muted);
    margin: 0 0 16px;
  }

  .caps-detail-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .caps-detail-list li {
    display: flex;
    align-items: baseline;
    gap: 10px;
    font-size: 0.92rem;
    color: var(--text-secondary);
    line-height: 1.5;
  }

  .cap-bullet {
    color: var(--accent-blue);
    font-weight: 700;
    flex-shrink: 0;
  }

  /* Sidebar Consultation Card */
  .consultation-card {
    background-color: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-sm);
    padding: 28px;
    display: flex;
    flex-direction: column;
    gap: 14px;
    position: sticky;
    top: 100px;
  }

  .consult-kicker {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    color: var(--accent-blue);
    text-transform: uppercase;
  }

  .consult-title {
    font-size: 1.3rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0;
  }

  .consult-desc {
    font-size: 0.88rem;
    color: var(--text-secondary);
    line-height: 1.55;
    margin: 0;
  }

  .governance-assurances-list {
    display: flex;
    flex-direction: column;
    gap: 8px;
    border-top: 1px solid var(--border-color);
    padding-top: 16px;
    margin-top: 6px;
  }

  .assurance-item {
    display: flex;
    align-items: baseline;
    gap: 8px;
    font-size: 0.8rem;
    color: var(--text-secondary);
    line-height: 1.4;
  }

  .assure-icon {
    color: var(--accent-blue);
    font-size: 0.85rem;
    font-weight: 700;
    flex-shrink: 0;
  }
</style>
