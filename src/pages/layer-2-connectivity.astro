---
// src/pages/layer-2-connectivity.astro
import Layout from '../layouts/Layout.astro';
---

<Layout 
  title="Layer 2 Private Ethernet & EVPN Sourcing — InfraHub"
  description="Source carrier-neutral Layer 2 point-to-point (E-Line) and multipoint (E-LAN) Ethernet connectivity. Private VLAN transport with zero public internet exposure."
  canonicalPath="/layer-2-connectivity"
>
  <div class="section section-hero network-overlay">
    <div class="container service-container">
      <div class="service-header">
        <div class="header-badge-row">
          <span class="pill-badge badge-blue">Private Networking</span>
          <span class="pill-badge badge-teal">Partner Delivered</span>
        </div>
        <h1 class="service-title">Layer 2 Private Ethernet Connectivity</h1>
        <p class="service-lead">
          Interconnect datacenters, corporate headquarters, and cloud environments over private, encrypted Layer 2 Ethernet circuits (E-Line / E-LAN) with deterministic latency and zero packet loss.
        </p>
        <div class="service-hero-actions">
          <a href="/lets-talk?service=layer-2" class="btn btn-accent btn-lg">Request Layer 2 Sourcing</a>
        </div>
      </div>

      <div class="content-section">
        <span class="section-sub-tag">Network Topologies</span>
        <h2>Available Layer 2 Topologies</h2>
        <div class="problem-grid">
          <div class="problem-card">
            <h3>Point-to-Point (E-Line)</h3>
            <p>Dedicated pseudowire linking two datacenter MMRs with 802.1Q VLAN tagging and transparent MTU (up to 9000 bytes jumbo frames).</p>
          </div>
          <div class="problem-card">
            <h3>Multipoint Ethernet (E-LAN)</h3>
            <p>Mesh multiple branch offices, public cloud VPCs, and colocation cages into a single unified broadcast domain using EVPN-VXLAN.</p>
          </div>
          <div class="problem-card">
            <h3>Software-Defined Cloud Fabrics</h3>
            <p>Spin up private virtual cross-connects dynamically through modern SDI fabrics without physical cabling delays.</p>
          </div>
        </div>
      </div>

      <div class="service-footer-cta text-center">
        <h2>Source Private Layer 2 Networks</h2>
        <a href="/lets-talk?service=layer-2" class="btn btn-accent btn-lg">Consult a Network Architect</a>
      </div>
    </div>
  </div>
</Layout>

<style>
  .service-container { max-width: 1040px; }
  .service-header { text-align: center; max-width: 820px; margin: 0 auto 64px; }
  .header-badge-row { display: flex; justify-content: center; gap: 8px; margin-bottom: 16px; }
  .service-title { font-size: clamp(2.2rem, 4vw, 3.4rem); font-weight: 800; line-height: 1.15; margin-bottom: 20px; }
  .service-lead { font-size: 1.15rem; color: var(--text-secondary); line-height: 1.65; margin-bottom: 32px; }
  .service-hero-actions { display: flex; justify-content: center; gap: 14px; }
  .content-section { margin-bottom: 72px; }
  .section-sub-tag { display: block; font-family: var(--font-heading); font-size: 0.8rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: var(--accent-blue); margin-bottom: 8px; }
  .content-section h2 { font-size: 1.85rem; font-weight: 700; margin-bottom: 18px; }
  .problem-grid { display: grid; grid-template-columns: 1fr; gap: 20px; }
  @media (min-width: 768px) { .problem-grid { grid-template-columns: repeat(3, 1fr); } }
  .problem-card { background-color: var(--bg-secondary); border: 1px solid var(--border-color); border-radius: var(--radius-lg); padding: 24px; }
  .problem-card h3 { font-size: 1.1rem; margin-bottom: 10px; color: var(--text-primary); }
  .problem-card p { font-size: 0.9rem; color: var(--text-secondary); line-height: 1.5; }
  .service-footer-cta { padding: 40px 0; display: flex; flex-direction: column; align-items: center; gap: 16px; }
</style>
