---
// src/components/PartnerTrustRibbon.astro | InfraHub Partner Ecosystem Ribbon — Premium Marquee Pass
import { getCollection } from 'astro:content';
import { logoMaxHeightStyle } from '../lib/partner-logo';

const allPartners = await getCollection('partners');

// Filter strictly for partners with confirmed relationship and approved naming for homepage marquee
const approvedPartners = allPartners
  .filter(p => 
    p.data.homepageMarqueeEnabled && 
    p.data.relationshipConfirmed && 
    p.data.publicNameApproved
  )
  .sort((a, b) => (a.data.homepagePriority ?? 10) - (b.data.homepagePriority ?? 10));

// Optical scale is a property of the artwork, so it lives with the partner record rather
// than in a second table here. A lockup whose wordmark is outlined or whose aspect ratio is
// unusual needs a nudge to carry the same visual weight as the others in a 156x48 slot.
// Optical sizing lives in src/lib/partner-logo.ts, because the ribbon, the catalogue and
// the profile header all need the same correction and had three different fixed heights.
const RIBBON_LOGO_HEIGHT = 32;

---

<section class="partner-trust-ribbon" aria-label="Partner Ecosystem">
  <!-- Subtle Route Line Entry Connector from Hero -->
  <div class="ribbon-route-entry" aria-hidden="true">
    <div class="route-entry-line"></div>
  </div>

  <div class="ribbon-inner">
    <div class="ribbon-fixed-zone">
      <span class="ribbon-kicker">Our partner ecosystem</span>
    </div>

    <div class="ribbon-track-wrapper">
      <div class="marquee-strip">
        <!-- Sequence A: Accessible Primary Sequence -->
        <ul class="marquee-track primary-track" aria-label="Partner ecosystem">
          {approvedPartners.map((partner, index) => (
            <li class="partner-item">
              <a 
                href={`/partners/${partner.slug}`} 
                class="partner-logo-link"
                aria-label={`${partner.data.name} — ${partner.data.shortRole || partner.data.tagline}`}
                title={`${partner.data.name} — ${partner.data.shortRole || partner.data.tagline}`}
              >
                {partner.data.logoApproved && partner.data.logoStatus === 'approved' && partner.data.logo ? (
                  <img 
                    src={partner.data.logo} 
                    alt={`${partner.data.name} logo`} 
                    class="partner-logo-img"
                    style={logoMaxHeightStyle(partner.data.logoScale, RIBBON_LOGO_HEIGHT)}
                    loading={index < 2 ? "eager" : "lazy"}
                    width="140"
                    height="32"
                  />
                ) : (
                  <span class="partner-text-mark">
                    <span class="partner-name">{partner.data.name}</span>
                    {partner.data.shortRole && <span class="partner-role">{partner.data.shortRole}</span>}
                  </span>
                )}
              </a>
            </li>
          ))}
        </ul>

        <!-- Sequence B: Seamless Duplicate (aria-hidden) -->
        <ul class="marquee-track duplicate-track" aria-hidden="true">
          {approvedPartners.map(partner => (
            <li class="partner-item">
              <span class="partner-logo-link decorative">
                <img 
                  src={partner.data.logo} 
                  alt="" 
                  class="partner-logo-img"
                  style={logoMaxHeightStyle(partner.data.logoScale, RIBBON_LOGO_HEIGHT)}
                  loading="lazy"
                  width="140"
                  height="32"
                />
              </span>
            </li>
          ))}
        </ul>

        <!-- Sequence C: Seamless Ultrawide Triplicate (aria-hidden) -->
        <ul class="duplicate-track" aria-hidden="true">
          {approvedPartners.map(partner => (
            <li class="partner-item">
              <span class="partner-logo-link decorative">
                <img 
                  src={partner.data.logo} 
                  alt="" 
                  class="partner-logo-img"
                  style={logoMaxHeightStyle(partner.data.logoScale, RIBBON_LOGO_HEIGHT)}
                  loading="lazy"
                  width="140"
                  height="32"
                />
              </span>
            </li>
          ))}
        </ul>
      </div>
    </div>
  </div>

  <!-- The route flattens into the structural axis of the partner ecosystem (Directives 10 & 11) -->
  <div class="ribbon-structural-axis" aria-hidden="true"></div>
</section>

<script>
  function initPartnerMarquee() {
    const wrapper = document.querySelector('.partner-trust-ribbon .ribbon-track-wrapper') as HTMLElement | null;
    if (!wrapper) return;
    const primaryTrack = wrapper.querySelector('.primary-track') as HTMLElement | null;
    if (!primaryTrack) return;

    const SPEED_PPS = 28; // Constant speed: 28 CSS px per second

    function measure() {
      if (!primaryTrack || !wrapper) return;
      const rect = primaryTrack.getBoundingClientRect();
      const width = rect.width;
      if (width <= 0) return;
      const duration = Math.max(40, width / SPEED_PPS);
      wrapper.style.setProperty('--marquee-distance', `${width.toFixed(2)}px`);
      wrapper.style.setProperty('--marquee-duration', `${duration.toFixed(2)}s`);
    }

    measure();

    if ('ResizeObserver' in window) {
      const ro = new ResizeObserver(() => measure());
      ro.observe(primaryTrack);
    }

    window.addEventListener('resize', measure, { passive: true });
    if (document.fonts && document.fonts.ready) {
      document.fonts.ready.then(measure);
    }
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initPartnerMarquee);
  } else {
    initPartnerMarquee();
  }
</script>

<style>
  .partner-trust-ribbon {
    position: relative;
    background-color: #ffffff;
    border-top: 1px solid var(--border-color, #e2e8f0);
    border-bottom: 1px solid var(--border-color, #e2e8f0);
    padding: 26px 0;
    overflow: hidden;
    z-index: 10;
  }

  /* Infrastructure Route Line Entry (connector from hero) */
  .ribbon-route-entry {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 2px;
    height: 16px;
    display: flex;
    justify-content: center;
    z-index: 2;
  }
  .route-entry-line {
    width: 1.5px;
    height: 100%;
    background-color: var(--accent-blue, #2563eb);
  }

  /* Structural axis: the route flattens into the marquee axis (Directives 10 & 11) */
  .ribbon-structural-axis {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(
      90deg,
      transparent 0,
      rgba(37, 99, 235, 0.45) clamp(178px, 13vw, 196px),
      rgba(37, 99, 235, 0.25) 50%,
      transparent 100%
    );
    pointer-events: none;
  }

  /* Composition: dedicated full-width horizontal geometry */
  .ribbon-inner {
    width: 100%;
    max-width: 100vw;
    overflow: hidden;
    margin: 0;
    padding-left: clamp(20px, 2vw, 28px);
    padding-right: 0;
    display: flex;
    flex-direction: column;
    gap: 16px;
    align-items: flex-start;
  }

  @media (min-width: 900px) {
    .ribbon-inner {
      display: grid;
      grid-template-columns: clamp(178px, 13vw, 196px) minmax(0, 1fr);
      align-items: center;
      column-gap: clamp(20px, 2vw, 28px);
      gap: 0;
    }
  }

  /* Quiet Fixed Label Zone */
  .ribbon-fixed-zone {
    width: 100%;
    display: flex;
    align-items: center;
    padding-bottom: 12px;
    border-bottom: 1px solid var(--border-color, #e2e8f0);
    flex-shrink: 0;
  }

  @media (min-width: 900px) {
    .ribbon-fixed-zone {
      width: auto;
      min-width: 0;
      padding: 0 clamp(18px, 1.8vw, 24px) 0 0;
      border-bottom: none;
      border-right: 1px solid var(--border-color, #e2e8f0);
      justify-content: flex-start;
    }
  }

  .ribbon-kicker {
    font-family: var(--font-heading, "IBM Plex Sans", -apple-system, BlinkMacSystemFont, sans-serif);
    font-size: clamp(0.92rem, 1.1vw, 1rem);
    font-weight: 600;
    color: var(--text-primary, #0f172a);
    letter-spacing: -0.01em;
    line-height: 1.35;
    white-space: nowrap;
    margin: 0;
  }

  /* Marquee Track Wrapper: edge mask + divider safe gap */
  .ribbon-track-wrapper {
    flex-grow: 1;
    overflow: hidden;
    position: relative;
    width: 100%;
    min-width: 0;
    max-width: 100%;
    padding-left: clamp(16px, 1.5vw, 24px);
    mask-image: linear-gradient(90deg, transparent 0, #000 18px, #000 calc(100% - 36px), transparent 100%);
    -webkit-mask-image: linear-gradient(90deg, transparent 0, #000 18px, #000 calc(100% - 36px), transparent 100%);
  }

  /* Parent strip: only this element translates */
  .marquee-strip {
    display: flex;
    width: max-content;
    will-change: transform;
    animation: partnerMarquee var(--marquee-duration, 52s) linear infinite;
  }

  @keyframes partnerMarquee {
    from {
      transform: translate3d(0, 0, 0);
    }
    to {
      transform: translate3d(calc(-1 * var(--marquee-distance, 1472px)), 0, 0);
    }
  }

  /* Pause smoothly on hover or focus */
  .ribbon-track-wrapper:hover .marquee-strip,
  .ribbon-track-wrapper:focus-within .marquee-strip {
    animation-play-state: paused;
  }

  /* Groups: exact 28px gap & 28px right padding guarantees seamless loop */
  .primary-track,
  .duplicate-track {
    display: flex;
    align-items: center;
    gap: 28px;
    padding-right: 28px;
    list-style: none;
    margin: 0;
    padding-left: 0;
    flex-shrink: 0;
  }

  /* Optical Slot: 156px wide x 48px high architectural rhythm */
  .partner-item {
    flex: 0 0 156px;
    height: 54px;
    display: flex;
    align-items: center;
    justify-content: center;
    box-sizing: border-box;
  }

  @media (max-width: 899px) {
    .partner-item {
      flex: 0 0 140px;
      height: 50px;
    }
  }

  /* Interactive link area */
  .partner-logo-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    height: 100%;
    min-height: 44px;
    padding: 4px 8px;
    border-radius: var(--radius-sm, 4px);
    text-decoration: none;
    box-sizing: border-box;
    transition: outline-color 160ms ease, background-color 160ms ease;
  }

  .partner-logo-link:hover,
  .partner-logo-link:focus-visible {
    outline: 2px solid var(--accent-blue, #2563eb);
    outline-offset: 2px;
  }

  .partner-logo-link.decorative {
    cursor: default;
    pointer-events: none;
  }

  /* Logos: opacity 1, natural aspect ratio contained in 142x32 box */
  .partner-logo-img {
    width: auto;
    height: auto;
    max-width: 142px;
    max-height: 32px;
    object-fit: contain;
    display: block;
    user-select: none;
    pointer-events: none;
    opacity: 1;
  }

  /* Text mark fallback */
  .partner-text-mark {
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 4px 10px;
    border-radius: var(--radius-sm, 4px);
    background: #f8fafc;
    border: 1px solid var(--border-color, #e2e8f0);
    text-align: center;
  }
  .partner-name {
    font-family: var(--font-heading, "IBM Plex Sans", sans-serif);
    font-size: 0.92rem;
    font-weight: 600;
    color: var(--text-primary, #0f172a);
    line-height: 1.15;
  }
  .partner-role {
    font-family: var(--font-mono, monospace);
    font-size: 0.68rem;
    font-weight: 500;
    color: #64748b;
    line-height: 1.2;
    margin-top: 2px;
  }

  /* Accessible Reduced Motion: static swipeable row */
  @media (prefers-reduced-motion: reduce) {
    .ribbon-track-wrapper {
      mask-image: none;
      -webkit-mask-image: none;
      overflow-x: auto;
      scroll-behavior: smooth;
      -webkit-overflow-scrolling: touch;
      padding-left: 0;
    }

    .marquee-strip {
      animation: none !important;
      transform: none !important;
      width: auto;
    }

    .marquee-track {
      animation: none !important;
      transform: none !important;
    }

    .duplicate-track {
      display: none !important;
    }

    .primary-track {
      overflow-x: auto;
      scroll-behavior: smooth;
      -webkit-overflow-scrolling: touch;
      padding: 4px 0;
    }
  }
</style>
